/*
import React from 'react';
import { View, ViewStyle } from 'react-native';
import { Calendar, CalendarHeaderForMonthViewProps, CalendarHeaderProps, CalendarTouchableOpacityProps, DateRangeHandler, EventCellStyle, EventRenderer, ICalendarEventBase, Mode, WeekNum } from 'react-native-big-calendar';

export interface CalendarComponentProps<T extends ICalendarEventBase> {
  events: T[];
  height: number;
  overlapOffset?: number;
  hourRowHeight?: number;
  ampm?: boolean;
  date?: Date;
  eventCellStyle?: EventCellStyle<T>;
  calendarContainerStyle?: ViewStyle;
  headerContainerStyle?: ViewStyle;
  bodyContainerStyle?: ViewStyle;
  renderEvent?: EventRenderer<T> | ((event: T, touchableOpacityProps: CalendarTouchableOpacityProps) => React.ReactElement | null);
  renderHeader?: React.ComponentType<CalendarHeaderProps<T> & { mode: Mode }>;
  renderHeaderForMonthView?: React.ComponentType<CalendarHeaderForMonthViewProps>;
  locale?: string;
  hideNowIndicator?: boolean;
  mode?: Mode;
  scrollOffsetMinutes?: number;
  showTime?: boolean;
  swipeEnabled?: boolean;
  weekStartsOn?: WeekNum;
  weekEndsOn?: WeekNum;
  onChangeDate?: DateRangeHandler;
  onPressCell?: (date: Date) => void;
  onPressDateHeader?: (date: Date) => void;
  //onPressEvent?: (event: ICalendarEventBase<T>) => void;
  eventMinHeightForMonthView?: number;
  activeDate?: Date;
  moreLabel?: string;
  showAdjacentMonths?: boolean;
  sortedMonthView?: boolean;
}
*/
  